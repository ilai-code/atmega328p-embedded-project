Small beginner project that utilizes `avr-gcc` in order to do bare metal programming for an `ATmega328p` microcontroller.

This project uses `VS Code` but you may use any code editer or IDE. In order to build the project you have to ensure that `avr-gcc` is installed on your machine. If you are not connecting to a microcontroller through WSL you may have to modify the makefile.